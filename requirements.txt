# Core
torch>=2.1.0
torchaudio>=2.1.0
numpy>=1.24
scipy>=1.10
librosa>=0.10
soundfile>=0.12

# STT / LID
transformers>=4.40        # Whisper v3, wav2vec2
sentencepiece

# Phonetic / translation
epitran                   # G2P fallback for English
indic-num2words
# indic-g2p (installed manually if available)

# TTS
# YourTTS / VITS are installed via TTS (coqui-tts) OR via MMS from fairseq.
# The code ships with both a PyTorch VITS reference and an MMS loader.
TTS; python_version<"3.12"

# DTW
fastdtw>=0.3.4
dtw-python>=1.3

# Denoising
# DeepFilterNet is optional; we always ship a spectral-subtraction fallback.
deepfilternet>=0.5; platform_system != "Windows"

# Metrics
jiwer                     # WER
pymcd                     # MCD
scikit-learn

# Utilities
tqdm
pyyaml
matplotlib
